apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pcd-redis-db-pvc
  namespace: icees-prod
spec:
  storageClassName: basic
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 8Gi

