version: '3'
services:
  icees-api-test:
    build:
      context: .
      dockerfile: test/Dockerfile
    image: icees-api-test:0.2.0
    command: ["-s", "-vv"] # , "-k", "test_feature_count_cohort_features_two_years"]
    environment:
      WAIT: 60
    depends_on:
      - db
      - server

